# Agent Resources

Reusable agent assets for this tool.

- **Skills** live in `.agents/skills/` — read the relevant skill before starting work in that domain
- **AGENTS.md** at the repo root defines what agents are and are not allowed to do
- **CLAUDE.md** at the repo root defines coding conventions and build commands
